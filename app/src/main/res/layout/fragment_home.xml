<?xml version="1.0" encoding="utf-8"?>
<layout
    xmlns:android="http://schemas.android.com/apk/res/android">
    <data>
        <import type="android.view.View"/>
        <variable
            name="viewModel"
            type="id.widiarifki.uebermaps.presentation.main.home.HomeViewModel" />
        <variable
            name="accountViewModel"
            type="id.widiarifki.uebermaps.presentation.main.account.AccountViewModel" />
    </data>
    <androidx.core.widget.NestedScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent">
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="vertical">

            <!-- Recommended -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                style="@style/section">
                <LinearLayout
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:orientation="horizontal"
                    style="@style/section_header">
                    <TextView
                        android:layout_width="0dp"
                        android:layout_height="wrap_content"
                        android:layout_weight="1"
                        android:text="@string/label_recommended"
                        style="@style/section_title"/>
                </LinearLayout>
                <RelativeLayout
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content">
                    <ProgressBar
                        android:layout_width="wrap_content"
                        android:layout_height="@dimen/home_list_item_card_height"
                        android:layout_centerInParent="true"
                        android:visibility="@{viewModel.recommendedMaps.loading ? View.VISIBLE : View.INVISIBLE, default=visible}"/>
                    <androidx.recyclerview.widget.RecyclerView
                        android:id="@+id/rvRecommendedMaps"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:visibility="@{viewModel.recommendedMaps.loading ? View.GONE : View.VISIBLE, default=gone}"/>
                </RelativeLayout>
            </LinearLayout>

            <!-- Latest/Trends  -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                style="@style/section">
                <LinearLayout
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:orientation="horizontal"
                    style="@style/section_header">
                    <TextView
                        android:layout_width="0dp"
                        android:layout_height="wrap_content"
                        android:layout_weight="1"
                        android:text="@string/label_latest"
                        style="@style/section_title"/>
                </LinearLayout>
                <RelativeLayout
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content">
                    <ProgressBar
                        android:layout_width="wrap_content"
                        android:layout_height="@dimen/home_list_item_card_height"
                        android:layout_centerInParent="true"
                        android:visibility="@{viewModel.latestMaps.loading ? View.VISIBLE : View.INVISIBLE, default=visible}"/>
                    <androidx.recyclerview.widget.RecyclerView
                        android:id="@+id/rvLatestMaps"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:visibility="@{viewModel.latestMaps.loading ? View.GONE : View.VISIBLE, default=gone}"/>
                </RelativeLayout>
            </LinearLayout>

            <!-- My Maps -->
            <!--<LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                style="@style/section"
                android:visibility="@{accountViewModel.userLogin.data != null ? View.VISIBLE : View.GONE, default=visible}">-->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                style="@style/section">
                <LinearLayout
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:orientation="horizontal"
                    style="@style/section_header">
                    <TextView
                        android:layout_width="0dp"
                        android:layout_height="wrap_content"
                        android:layout_weight="1"
                        android:text="@string/label_my_maps"
                        style="@style/section_title"/>
                </LinearLayout>
                <RelativeLayout
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content">
                    <ProgressBar
                        android:layout_width="wrap_content"
                        android:layout_height="@dimen/home_list_item_card_height"
                        android:layout_centerInParent="true"
                        android:visibility="@{viewModel.myMaps.loading ? View.VISIBLE : View.INVISIBLE, default=visible}"/>
                    <androidx.recyclerview.widget.RecyclerView
                        android:id="@+id/rvMyMaps"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:visibility="@{viewModel.myMaps.loading ? View.GONE : View.VISIBLE, default=gone}"/>
                    <LinearLayout
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:layout_centerInParent="true"
                        android:orientation="vertical"
                        android:gravity="center"
                        android:visibility="@{!viewModel.myMaps.loading &amp;&amp; (viewModel.myMaps.empty || viewModel.myMaps.error) ? View.VISIBLE : View.GONE, default=visible}">
                        <TextView
                            android:layout_width="wrap_content"
                            android:layout_height="wrap_content"
                            android:text="@{viewModel.myMaps.empty ? @string/empty_my_maps : viewModel.myMaps.message, default=@string/empty_my_maps}"
                            style="@style/TextAppearance.AppCompat.Medium"/>
                        <Button
                            android:layout_width="wrap_content"
                            android:layout_height="wrap_content"
                            android:text="@string/add_my_map"
                            android:visibility="@{viewModel.myMaps.empty ? View.VISIBLE : View.GONE}"/>
                    </LinearLayout>
                </RelativeLayout>
            </LinearLayout>


        </LinearLayout>
    </androidx.core.widget.NestedScrollView>
</layout>